/**
 * google-drive.js — DEPRECATED (2026-05-17).
 *
 * The auto-create-Shared-Drive-per-client experiment is on hold. The founder
 * creates the Shared Drive manually, then pastes the URL into the client's
 * drive_folder_url field via the cockpit.
 *
 * This file is intentionally left as a stub so old git diffs still resolve.
 * Nothing imports it. Safe to delete in a future cleanup.
 */
export {};
