# Redirect www to non-www (301 permanent redirect)
https://www.shinelstudios.in/* https://shinelstudios.in/:splat 301!

# Redirect HTTP to HTTPS
http://shinelstudios.in/* https://shinelstudios.in/:splat 301!
http://www.shinelstudios.in/* https://shinelstudios.in/:splat 301!

# Legacy Page Redirects
# /work/* was previously redirected to /portfolio/:splat; removed because
# /work is now the live Work page and /portfolio/:slug is the team-member
# microsite. A splat redirect collided those two surfaces.
/forgot-password   /login    301

# SPA fallback (must be last)
/*    /index.html   200
